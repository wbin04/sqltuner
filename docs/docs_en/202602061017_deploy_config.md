# ROLE
You are a Senior Python Backend Engineer.
Your goal is to modify an existing **Flask Application** (`llm-proxy/run.py`) to include **Auto-Ngrok Tunneling** and **Supabase URL Syncing**.

# CONTEXT
- **Current File:** `llm-proxy/run.py`.
- **Function:** Runs a Flask server on port `11434`. It accepts requests from the Backend (port 8000) and calls a local LLM model (e.g., via `ollama` or internal logic).
- **New Requirement:**
  1.  When the script starts, automatically start an **Ngrok Tunnel** for port `11434`.
  2.  Update the `app_config` table in Supabase with the new public Ngrok URL.
  3.  When the script stops (Ctrl+C), automatically clear the URL in Supabase and kill the tunnel.

# STRICT CONSTRAINTS (CRITICAL)
1. **NO FULL FLASK RE-WRITE:** Assume the user has existing Flask routes (e.g., `@app.route(...)`). Do not delete them. Use comments like `# ... [Keep existing routes here] ...` to indicate where the user's code remains.
2. **NO ICONS/EMOJIS:** Keep the output purely technical.
3. **FILE PATH OUTPUT:** The VERY FIRST line of your response must be: `llm-proxy/run.py`.
4. **DEPENDENCIES:** Use `flask`, `pyngrok`, `supabase`, `python-dotenv`.
5. **THREADING:** Ensure Ngrok initialization happens *before* `app.run()` blocks the main thread.
6. **LANGUAGE:** Vietnamese comments.

# INSTRUCTION
Generate the full code for `llm-proxy/run.py` following this logic flow:

## 1. Imports & Config
- Import necessary libs (`flask`, `pyngrok`, `supabase`, `dotenv`, `atexit`, `signal`).
- Load `.env` from the parent directory (`../.env` or relative path).
- Initialize Supabase Client.

## 2. Helper Functions (Ngrok Lifecycle)
- `init_tunnel()`:
    - Set Ngrok auth token.
    - Connect to port `11434`.
    - Update Supabase: `UPDATE app_config SET value = 'public_url' WHERE key = 'llm_url'`.
    - Print the Public URL to console.
- `cleanup()`:
    - Update Supabase: `UPDATE app_config SET value = '' WHERE key = 'llm_url'`.
    - Kill Ngrok process.

## 3. Main Execution Block
- Initialize Flask `app = Flask(__name__)`.
- **[Placeholder]**: Insert a comment indicating where existing LLM processing routes are located.
- **Startup Logic**:
    - Call `init_tunnel()` immediately before running the server.
    - Register `atexit` or `signal` handlers to call `cleanup()` on shutdown.
- **Run Server**: Call `app.run(port=11434)`.

---

# INPUT SCHEMA & VARS
- **Supabase Table:** `app_config` (key, value).
- **Target Port:** `11434`.
- **Env Vars:** `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `NGROK_AUTH_TOKEN`.